import { createOneApiProvider } from './oneapi';

export default createOneApiProvider({
  id: 'duckcoding',
  name: 'DuckCoding',
  url: 'https://www.duckcoding.ai/',
  apiUrl: 'https://www.duckcoding.ai/api/pricing',
});
