import { createOneApiProvider } from './oneapi';

export default createOneApiProvider({
  id: 'packycode',
  name: 'PackyCode',
  url: 'https://www.packyapi.com/',
  apiUrl: 'https://www.packyapi.com/api/pricing',
});
