import { createOneApiProvider } from './oneapi';

export default createOneApiProvider({
  id: 'magic666',
  name: '即刻AI',
  url: 'https://magic666.top/',
  apiUrl: 'https://magic666.top/api/pricing',
});
