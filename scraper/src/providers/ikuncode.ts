import { createOneApiProvider } from './oneapi';

export default createOneApiProvider({
  id: 'ikuncode',
  name: 'iKunCode',
  url: 'https://api.ikuncode.cc/',
  apiUrl: 'https://api.ikuncode.cc/api/pricing',
});
